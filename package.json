{
    "name": "typescript-challenges",
    "version": "1.0.0",
    "description": "A collection of TypeScript challenges.",
    "type": "module",
    "devDependencies": {
        "cross-env": "^7.0.3",
        "jest": "^29.2.1",
        "typescript": "^4.8.4"
    },
    "scripts": {
        "build": "npx tsc -b",
        "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
        "integrate": "npm run build && npm test"
    },
    "optionalDependencies": {
        "fsevents": "^2.3.2"
    }
}
