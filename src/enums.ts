export enum Implementation {
    for           = 'for',
    while         = 'while',
    recursive     = 'recursive',
    tailRecursive = 'tailRecursive',
    reduce        = 'reduce',
    superReduce   = 'superReduce'
}